PORT=5000

# Option 1: single connection string
DATABASE_URL=postgresql://username:password@localhost:5432/educore_lms

# Option 2: individual settings (used if DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_NAME=educore_lms

JWT_SECRET=replace_this_with_a_long_random_secret_string
